DATA B
DATA A
DATA C[4]
DATA D
CONST E = 7
START:
READ AX
READ BX
MOV A, AX
MOV B, BX
ADD CX, AX, BX
MOV DX, E
X:
IF CX EQ DX THEN
	MOV C[0], CX
	MOV D, CX
ELSE
	MOV C[1], CX
IFEND
JUMP X
PRINT AX
PRINT BX
PRINT CX
PRINT DX
END